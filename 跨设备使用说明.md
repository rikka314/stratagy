# 在其他设备使用说明

## ⚠️ 重要提示

虚拟环境（`.venv` 文件夹）包含设备特定路径，**不能直接复制使用**。

## 📦 正确的分享方式

### 方法 1：不包含虚拟环境（推荐）

压缩时**排除** `.venv` 文件夹，只分享：
```
stratagy/
├── setup.bat
├── start.bat
├── app.py
├── requirements.txt
├── data/
└── 其他文件...
```

对方收到后：
1. 解压
2. 双击 `setup.bat`（会自动创建新的虚拟环境）
3. 双击 `start.bat`

### 方法 2：如果已经包含了 .venv

收到文件后：
1. **删除** `.venv` 文件夹
2. 双击 `setup.bat`（重新创建）
3. 双击 `start.bat`

## ❌ 常见错误

**错误信息**：
```
Fatal error in launcher: Unable to create process using 
'"C:\...\原设备路径\.venv\Scripts\python.exe"'
```

**原因**：虚拟环境绑定到原设备路径

**解决**：删除 `.venv` 并重新运行 `setup.bat`

## 🎯 为什么不能直接用

Python 虚拟环境的工作原理：
- 创建时记录了 Python 解释器的**绝对路径**
- 移动到其他设备后，该路径不存在
- 必须在新设备上重新创建

## ✅ 正确流程总结

**分享者**：
```bash
# 压缩时排除 .venv
不要打包 .venv 文件夹
```

**接收者**：
```bash
1. 解压文件
2. 双击 setup.bat（2-3分钟）
3. 双击 start.bat
```

每个设备都需要运行一次 `setup.bat`，这是正常的！
