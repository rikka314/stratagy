# Gate 0 Acceptance Record — 2026-08-03

## Scope

Gate 0 establishes a runnable test environment, freezes the cross-window market,
execution-cost, and research-output contracts, and assigns file ownership before
parallel implementation begins.

## Frozen inputs

- Development dependency lock: `requirements-dev.txt` includes the runtime
  requirements and pins `pytest==8.4.1`.
- Contract source: `document/interfaces/deans-award-research-contract.md`.
- Existing uncommitted change: `core/news_factor.py` is owned by Window 4 and is
  deliberately excluded from Window 0 edits.

## Test baseline

| Item | Result |
|---|---|
| Interpreter | Python 3.13.7 |
| Command | `.\\.venv\\Scripts\\python.exe -m pytest` |
| Passed / failed / skipped | 55 / 5 / 0 |
| Environment | `pytest==8.4.1`; the full runtime dependency set from `requirements.txt` is installed in `.venv` |
| Test temporary directory | `tests/.tmp_pytest` (the prior `.codex_pytest_tmp` directory is inaccessible on this host) |
| Notes | Baseline is recorded before parallel changes; failures must be classified as pre-existing or introduced. Streamlit emits harmless bare-mode `ScriptRunContext` warnings during UI tests. |

## Known baseline failures (not amended by Window 0)

| Owner | Test | Cause observed at baseline |
|---|---|---|
| Window 3 / adaptive-regime owner | `tests/test_adaptive_regime.py::test_train_adaptive_state_model_falls_back_to_logistic_without_lightgbm` | Installed `scikit-learn==1.8.0` no longer accepts `LogisticRegression(multi_class=...)`. Resolve with a compatible implementation or a deliberate upper version bound. |
| Window 1 / 5 | `tests/test_model_evaluation_page.py::test_normalize_model_evaluation_payload_handles_missing_optional_fields` | Test expects Chinese `未记录 MLflow`; the current UI emits English `Not Logged to MLflow`. |
| Window 1 | `tests/test_model_evaluation_page.py::test_home_page_contains_model_evaluation_cta` | Test expects Chinese CTA `进入模型评估`; the current UI is English. |
| Window 5 | `tests/test_model_evaluation_page.py::test_render_model_evaluation_page_minimal_render` | Evaluation-page rendering assertion is stale after the English UI switch. |
| Window 5 | `tests/test_w8_exports_and_routes.py::test_single_stock_export_html_uses_w8_site_shell` | Export-shell assertion is stale after the English UI switch. |

## Gate decision

**PASS with recorded baseline defects.** The Gate 0 entry conditions are met:
pytest is runnable, the pass/fail/skip counts are recorded, the Window 4 news
fix has an explicit owner, and the cross-window contract and ownership table are
frozen. Windows 1, 2, 3A, and 4 may proceed only within that table. The five
baseline failures must remain visible and be rechecked at Gate 1; they are not
permission to change another window's files.
