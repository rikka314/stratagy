# Gate 1 Acceptance Record — 2026-08-03

## Scope

Gate 1 integrates the frozen execution-cost contract with the dual-market
offline research runner, verifies the news-factor regression suite, and proves
that minimal US and CN_A runs produce reproducible reports.

## Delivered

- `MarketExecutionConfig` remains opt-in for normal UI/backtest calls. The
  model-test runner applies its frozen market costs through a context-local
  research scope, including nested strategy-pipeline backtests.
- `ResearchConfig` persists `commission_bps` and `slippage_bps`; omitted values
  use US `1 + 2 bps` and CN_A `3 + 5 bps`. Explicit zero values preserve the
  regression mode.
- Each completed model-test run now writes `config_snapshot.json` and
  `data_manifest.json`, including code revision marker, seed, data range,
  train/test split boundaries, market, and effective costs.
- Gate-1 smoke configurations are
  `model-test/configs/smoke_us_deans_gate1.json` and
  `model-test/configs/smoke_cn_a_deans_gate1.json`.

## Verification

| Check | Result |
|---|---|
| `pytest tests/test_backtest_market_rules.py tests/test_model_test_runner.py tests/test_news_factor.py -q` | 42 passed |
| US smoke (`AAPL`) | success; readable `report.json`, `config_snapshot.json`, and `data_manifest.json` |
| CN_A smoke (`600519`) | success; readable `report.json`, `config_snapshot.json`, and `data_manifest.json` |
| Full `pytest -q` | 74 passed / 2 failed / 0 skipped |

Smoke output directories are ignored runtime artifacts:

- `model-test/outputs/smoke_us_deans_gate1/`
- `model-test/outputs/smoke_cn_a_deans_gate1/`

## Remaining known failures

| Test | Ownership / cause | Gate-1 effect |
|---|---|---|
| `tests/test_adaptive_regime.py::test_train_adaptive_state_model_falls_back_to_logistic_without_lightgbm` | Existing `scikit-learn==1.8.0` incompatibility with `LogisticRegression(multi_class=...)`. | Outside the Gate-1 cost, runner, and news paths. |
| `tests/test_w8_exports_and_routes.py::test_single_stock_export_html_uses_w8_site_shell` | Existing stale Chinese export-shell expectation after the English UI switch. | Outside the offline-research smoke paths. |

## Gate decision

**PASS with two recorded, non-Gate-1 failures.** Zero-cost compatibility,
cost-aware dual-market runner behavior, data-manifest reproducibility, news
regression coverage, and US/CN_A smoke reports are verified. The two remaining
failures must be addressed or explicitly retained at Gate 2.
