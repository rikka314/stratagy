# W4 样本外验证记录

> 记录日期：2026-03-26  
> 目标：完成 `plan/week4.md` 大任务 4 的最小留痕，给出 AAPL 与 600519 两组样本外对比结果。

---

## 1. 验证口径

- 执行链路：复用 `ui/single_stock.py` 的 `_compute_test_model_payloads_cached()`，与当前单股页“测试集表现”区块保持同口径。
- 预设参数：`均衡策略（默认）`
- 日期窗口：沿用侧边栏默认最近 3 年区间，即以 2026-03-26 为基准的 `2023-03-27 ~ 2026-03-26`；若本地样本最后交易日早于该日期，则按实际数据结尾截断。
- 切分方式：时间顺序 `70%` 训练、`30%` 测试。
- 复权方式：`qfq`
- 回测参数：`stop_loss_mult=2.0`，`take_profit_mult=4.0`
- 对比模型：`Naive / SM / FSM / ML-LR / ML-LGBM`
- 结果状态：两组样本在本次执行中均无 payload warning。

数据来源：

- `AAPL`：仓库内 `data/aapl_clean_daily.csv`，本地可用最后交易日为 `2026-01-21`
- `600519`：`fetch_a_stock()` 本地缓存的前复权日线，缓存可用最后交易日为 `2026-03-23`

---

## 2. AAPL

样本范围：

- 全窗口：`2023-03-27 ~ 2026-01-21`
- 训练集：`2023-03-27 ~ 2025-03-17`，`495` 行
- 测试集：`2025-03-18 ~ 2026-01-21`，`213` 行

| 模型 | 年化收益 | 夏普 | 最大回撤 | 胜率 | 相对 Naive |
|------|---------:|-----:|---------:|-----:|-----------|
| Naive | 20.25% | 0.73 | -23.07% | 100.00% | 基线 |
| SM | -5.97% | -0.32 | -14.06% | 46.67% | 未跑赢 |
| FSM | -6.90% | -0.52 | -8.32% | 40.00% | 未跑赢 |
| ML-LR | 0.62% | 0.11 | -9.82% | 80.00% | 未跑赢 |
| ML-LGBM | -7.36% | -0.59 | -9.59% | 50.00% | 未跑赢 |

补充观察：

- AAPL 测试段属于偏强单边上涨区间，`Naive` 对这类窗口天然更占优。
- 增强模型整体都显著压低了回撤，但同时牺牲了过多上涨暴露，因此在收益与夏普上都没有追上 `Naive`。
- 两个 ML 变体都能稳定跑通，但这组样本不足以支持“ML-SM 在强趋势美股上优于买入持有”的结论。

ML 质量摘录：

- `ML-LR`：Precision `60.00%`，Recall `33.33%`，F1 `42.86%`，PR-AUC `0.616`，通过率 `33.33%`
- `ML-LGBM`：Precision `50.00%`，Recall `33.33%`，F1 `40.00%`，PR-AUC `0.510`，通过率 `40.00%`

---

## 3. 600519

样本范围：

- 全窗口：`2023-03-27 ~ 2026-03-23`
- 训练集：`2023-03-27 ~ 2025-04-28`，`506` 行
- 测试集：`2025-04-29 ~ 2026-03-23`，`217` 行

| 模型 | 年化收益 | 夏普 | 最大回撤 | 胜率 | 相对 Naive |
|------|---------:|-----:|---------:|-----:|-----------|
| Naive | -6.53% | -0.26 | -16.40% | 0.00% | 基线 |
| SM | -0.24% | 0.02 | -8.79% | 28.57% | 跑赢 |
| FSM | -0.50% | -0.01 | -8.37% | 33.33% | 跑赢 |
| ML-LR | -0.26% | 0.00 | -7.79% | 33.33% | 跑赢 |
| ML-LGBM | 3.02% | 0.44 | -5.18% | 37.50% | 跑赢 |

补充观察：

- 600519 测试段本身对 `Naive` 不友好，四条增强路径都明显压缩了回撤，并在年化收益上超过基线。
- `ML-LGBM` 是这组样本中最强的增强模型，收益、夏普和回撤三项同时领先。
- `SM / FSM / ML-LR` 虽然年化收益仍略为负值，但相对 `Naive` 的负收益窗口已经形成改进，因此可视为“在该样本上有效降低损失”。

ML 质量摘录：

- `ML-LR`：Precision `28.57%`，Recall `66.67%`，F1 `40.00%`，PR-AUC `0.304`，通过率 `63.64%`
- `ML-LGBM`：Precision `28.57%`，Recall `66.67%`，F1 `40.00%`，PR-AUC `0.611`，通过率 `63.64%`

---

## 4. 收口结论

- W4 大任务 4 的最低交付已完成：两组样本外验证结果已留痕，且 `MODULE_INTERFACES.md` 与 `AI_CONTEXT.md` 已同步。
- 当前结果不支持“增强模型在所有样本上都能跑赢 `Naive`”的泛化结论。
- 可接受的收口表述应为：
  - `FSM / ML-SM` 功能链路已跑通。
  - 样本外表现具有标的依赖性。
  - 在 `AAPL` 这类强趋势窗口中，`Naive` 仍可能是更强基线。
  - 在 `600519` 这类基线较弱的窗口中，增强模型已展示出改善空间，且 `ML-LGBM` 最优。
