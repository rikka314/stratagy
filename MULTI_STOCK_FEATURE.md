# 📊 多股票对比功能使用指南

## 🎯 功能概述

新增的多股票对比功能允许您同时分析和对比多只股票的表现，包括：
- 归一化价格走势对比
- 收益率相关性分析
- 详细统计数据对比
- 交互式图表操作

## 🚀 使用方法

### 1. 添加股票到数据库

在侧边栏找到 **"🔍 多股票对比"** 部分：

1. 点击 **"➕ 添加股票"** 展开面板
2. 在输入框中输入股票代码（如：TSLA, NVDA, MSFT）
3. 点击 **"添加到对比列表"** 按钮
4. 系统会自动：
   - 检查本地是否已有该股票数据
   - 如果没有，自动从 AkShare 下载并保存到 `data/` 目录
   - 提示添加成功

### 2. 选择对比股票

在 **"选择对比股票"** 多选框中：
- 从下拉列表选择要对比的股票
- 可以选择 2 只或更多股票
- 选中后会显示：✓ 已选择 N 只股票进行对比

### 3. 查看对比分析

选择 2 只或以上股票后，主页面会自动显示 **"📈 多股票价格对比分析"** 区域：

#### 归一化价格图表
- **起点统一为 100**：所有股票的起始价格都标准化为 100，方便直观对比涨跌幅
- **交互功能**：
  - 悬停查看详细信息（日期、归一化价格、期间收益）
  - 点击图例隐藏/显示特定股票
  - 使用时间选择器快速切换时间范围（1周、1月、3月、6月、1年、全部）
  - 拖动/缩放查看特定时间段
  - 使用范围滑块（底部）调整显示区间

#### 关键指标卡片
显示三个核心指标：
- **对比股票数量**：当前选中的股票总数
- **平均数据天数**：所有股票的平均数据记录数
- **最佳表现**：期间内收益率最高的股票及其收益率

#### 详细统计对比表
点击 **"📊 详细统计对比"** 展开面板查看：
- 起始价格与最新价格
- 总收益率
- 年化波动率
- 最高价与最低价
- 数据天数

#### 相关性热图（2只以上股票）
- 显示股票之间的收益率相关系数
- 颜色编码：
  - 红色：正相关（走势相似）
  - 蓝色：负相关（走势相反）
  - 白色：无相关性
- 数值范围：-1（完全负相关）到 +1（完全正相关）

## 💡 使用技巧

### 1. 数据管理
- **本地缓存**：下载的数据会保存在 `data/` 目录，下次使用时无需重新下载
- **数据更新**：如需更新数据，可在主股票分析中点击"下载/刷新 AkShare 数据"

### 2. 对比分析建议
- **同行业对比**：选择同一行业的股票（如：AAPL, MSFT, GOOGL）分析行业趋势
- **跨行业对比**：选择不同行业的股票分析投资组合多样性
- **指数对比**：添加指数ETF（如：SPY）作为市场基准
- **关注相关性**：相关性低的股票组合有助于分散风险

### 3. 图表交互技巧
- **双击图例**：单独显示某只股票
- **框选缩放**：在图表上拖动鼠标框选区域放大
- **双击图表**：重置缩放到完整视图
- **下载图表**：点击右上角相机图标保存为高清PNG（1200x800px，2x分辨率）

## 🎨 图表特性

### 专业金融风格
- **配色方案**：使用10种高区分度的颜色
- **清晰网格**：浅灰色网格线，不干扰数据查看
- **基准线**：虚线标注起点基准（100）
- **图例信息**：显示股票代码和期间收益率

### 高级交互
- **统一悬停模式**：鼠标悬停时同时显示所有股票在该时间点的数据
- **范围选择器**：快速切换常用时间范围
- **范围滑块**：精确控制显示的时间区间
- **响应式设计**：自动适应不同屏幕尺寸

## 📝 示例工作流

### 示例1：科技股对比分析
```
1. 添加股票：AAPL, MSFT, GOOGL, NVDA
2. 选择全部4只股票
3. 查看归一化图表，观察谁的涨幅最大
4. 查看相关性热图，了解它们之间的联动关系
5. 在详细统计中对比波动率，评估风险
```

### 示例2：投资组合优化
```
1. 添加候选股票：5-10只不同行业的股票
2. 选择全部股票进行对比
3. 在相关性热图中寻找低相关性的股票对
4. 选择相关性低但收益率高的股票构建组合
```

## 🔧 技术细节

### 归一化算法
```python
归一化价格 = (当前价格 / 起始价格) × 100
```

### 相关性计算
- 使用每日收益率计算Pearson相关系数
- 公式：`corr(stock_A_returns, stock_B_returns)`

### 数据来源
- 使用 AkShare API 获取美股日线数据
- 数据包含：开盘价、最高价、最低价、收盘价、成交量
- 支持前复权(qfq)、后复权(hfq)、不复权(none)

## ⚠️ 注意事项

1. **时间对齐**：不同股票的交易日期可能略有差异，系统会自动处理
2. **数据质量**：首次下载可能较慢，请耐心等待
3. **内存占用**：同时加载大量股票（>10只）可能影响性能
4. **网络要求**：下载新股票数据需要网络连接

## 🆕 未来计划

- [ ] 支持更多市场（A股、港股、加密货币）
- [ ] 添加策略对比功能（不同股票使用相同策略的表现）
- [ ] 导出对比报告（PDF/Excel）
- [ ] 自定义对比指标（PE、PB、股息率等）
- [ ] 行业板块分组对比

## 📞 技术支持

如有问题或建议，请查看：
- `STRATEGY_ANALYSIS.md` - 策略分析文档
- `DIAGNOSIS_TASKS.md` - 诊断任务说明
- `PROJECT_LOG.md` - 项目日志

---

**版本**：v2.0  
**更新日期**：2026-01-28  
**作者**：策略实验室团队
